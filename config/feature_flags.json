{
  "featureFlags": {
    "enableStrictValidation": true,
    "enableSchemaDriftCheck": false,
    "enableAuditLogging": true
  },
  "description": "Feature flags allow pipeline behavior to change without creating separate branches for every environment."
}
